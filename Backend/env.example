# MongoDB Configuration
# IMPORTANT: The backend server requires a MongoDB connection to start.
# Set MONGODB_URI to your MongoDB connection string, or leave empty to use local MongoDB default
# Local MongoDB default: mongodb://127.0.0.1:27017/neattend_local
# 
# To use MongoDB:
# 1. Install MongoDB: https://www.mongodb.com/try/download/community
# 2. Start MongoDB service
# 3. Set MONGODB_URI below (or leave empty for local default)
#
# Alternative: Use simple-server.js (no MongoDB required) by running: npm run dev:simple
MONGODB_URI=mongodb://localhost:27017/neattend

# Server Configuration
# IMPORTANT: This port must match the proxy target in vite.config.js (currently 8000)
PORT=8000

# CORS Configuration
# Comma-separated list of allowed origins for CORS and Socket.IO
# Example: CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,https://yourdomain.com
# If not set, defaults to development origins (localhost:5173, localhost:3000, etc.)
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
# Legacy support: CORS_ORIGIN (single origin) is also supported
# CORS_ORIGIN=http://localhost:5173

# JWT Configuration (for future authentication)
JWT_SECRET=your-super-secret-jwt-key-here
JWT_EXPIRES_IN=7d

# SMTP Email (optional)
# Leave MAIL_HOST / MAIL_PORT / MAIL_USER / MAIL_PASS blank to disable email sending.
# When disabled, verification codes are logged to the console instead of being emailed.
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_SECURE=false
MAIL_REQUIRE_TLS=true
MAIL_USER=your-smtp-username@example.com
MAIL_PASS=your-smtp-password-or-app-password
MAIL_FROM="NE-Attend <no-reply@neattend.local>"
# Set to true to warm up and keep the SMTP pool alive (disable by default to avoid timeouts in dev).
MAIL_WARMUP=false
MAIL_POOL=false
MAIL_CONNECTION_TIMEOUT=15000
MAIL_GREETING_TIMEOUT=10000
MAIL_SOCKET_TIMEOUT=15000
MAIL_KEEP_ALIVE_MS=120000
